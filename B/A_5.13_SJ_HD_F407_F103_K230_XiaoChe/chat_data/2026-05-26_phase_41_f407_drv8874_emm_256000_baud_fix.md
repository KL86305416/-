# Phase 41

- Date: `2026-05-26`
- Topic: `f407_drv8874_emm_256000_baud_fix`
- Trigger:
  - The motor side was confirmed as `Emm` firmware over UART with baud rate `256000`.
- Result:
  - The DRV8874 F407 firmware motor links were updated:
    - `UART5 -> 256000`
    - `USART6 -> 256000`
  - The OpenMV vision link on `UART4` was left unchanged at `115200`.
  - The CubeMX `.ioc` file was updated as well so the motor baud setting is not silently reverted on regeneration.
- Expected impact:
  - Removes the direct baud mismatch between the F407 motor-control firmware and the two X42S `Emm` UART links.
