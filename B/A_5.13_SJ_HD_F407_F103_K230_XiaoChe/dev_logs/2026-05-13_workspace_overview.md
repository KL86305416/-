# 工作区说明文档

- 日期：2026-05-13
- 工作区：`D:\A_5.13_SJ_HD_F407_F103_K230_XiaoChe`
- 目的：对当前工作区的组成、成熟度和工程状态做一份正式记录。

## 1. 根目录结构

| 路径 | 当前状态 | 当前作用 |
| --- | --- | --- |
| `F103 grayscale sensor(HD)` | 有实质内容 | STM32F103 灰度相关子工程 |
| `F407 main control panel` | 有实质内容 | STM32F407 主控板工程 |
| `K230 Visual module(SJ)` | 空 | 预留给 K230 视觉侧代码或资料 |
| `OpenMV Visual module(SJ)` | 空 | 预留给 OpenMV 视觉侧代码或资料 |
| `dev_logs` | 本次开始使用 | 开发日志和正式分析文档存放区 |
| `chat_data` | 空 | 预留给对话数据或导出记录 |
| `backups` | 只有结构 | 备份总区，已按模块拆分子区 |
| `temp_transfer` | 空 | 临时中转区 |
| `reference_library` | 空 | 资料库、手册、参考资料存放区 |

## 2. 工作区整体特征

当前工作区有几个明显特征：

- 它更像“工程资料目录”，不是一个整理完善的软件仓库。
- 根目录和子目录中都没有发现 `.git` 仓库。
- 没有发现 `README`、Markdown 说明、PDF 手册、图片、表格等辅助文档。
- 现有有效内容几乎全部集中在 `F103` 和 `F407` 两个 STM32 工程里。
- `K230` 和 `OpenMV` 目录目前只是结构预留，还没有放入实际代码或资料。

## 3. F103 模块概况

### 3.1 目录组成

主路径：

- `F103 grayscale sensor(HD)\code\4.28_test`
- `F103 grayscale sensor(HD)\PCB schematic diagram`

工程特征：

- MCU：`STM32F103C8T6`
- 工程形式：`STM32CubeMX + Keil MDK-ARM`
- 关键工程文件：
  - `4.28_test.ioc`
  - `MDK-ARM\4.28_test.uvprojx`
  - `MDK-ARM\4.28_test.uvoptx`

估算体量：

- 总文件数：约 `151`
- 总大小：约 `22.2 MB`
- `Core`：`13` 个文件，约 `59 KB`
- `Drivers`：`67` 个文件，约 `4.0 MB`
- `MDK-ARM`：`67` 个文件，约 `17.8 MB`

结论：

- 这个工程的用户代码体量很小。
- 主要体量来自 ST 驱动和 Keil 编译产物。

### 3.2 代码成熟度

当前 `Core\Src\main.c` 的流程很简单：

- 初始化 `GPIO`
- 初始化 `I2C2`
- 初始化 `USART1`
- 进入死循环

当前循环里的实际逻辑只有：

- 轮询 `PA11`
- 做一次简单消抖
- 当 `PA11` 为低电平时拉高 `PB9`
- 松开后再拉低 `PB9`

这说明当前提交进来的 `F103` 工程更接近：

- 板级点亮/连线验证工程
- 基础 IO 测试工程

而不是已经完成的灰度采集与处理应用。

### 3.3 硬件资料情况

原理图目录目前只有专有源文件：

- `F103 grayscale sensor.enet`
- `F103 grayscale sensor.epro2`

当前没有导出的 PDF 原理图，也没有图片版预览，因此无法只靠可读文本直接确认原理图语义。

## 4. F407 模块概况

### 4.1 目录组成

主路径：

- `F407 main control panel\code\AAA_F407VET6_5.6_Test`
- `F407 main control panel\PCB schematic diagram`

工程特征：

- MCU：`STM32F407VET6`
- 工程形式：`STM32CubeMX + Keil MDK-ARM`
- 关键工程文件：
  - `F407VET6_5.1_Test.ioc`
  - `MDK-ARM\F407VET6_5.1_Test.uvprojx`
  - `MDK-ARM\F407VET6_5.1_Test.uvoptx`

估算体量：

- 总文件数：约 `218`
- 总大小：约 `57.9 MB`
- `Core`：`21` 个文件，约 `124 KB`
- `Drivers`：`92` 个文件，约 `7.6 MB`
- `MDK-ARM`：`101` 个文件，约 `49.7 MB`

结论：

- 这个工程已经配置了很多外设。
- 但应用层逻辑仍然很少。
- 目录体量的大头仍然是驱动和编译产物。

### 4.2 代码成熟度

当前启动阶段已初始化的外设很多：

- `I2C1 / I2C2 / I2C3`
- `SPI1 / SPI2`
- `TIM1 / TIM2 / TIM3 / TIM4 / TIM12`
- `UART4 / UART5 / USART3 / USART6`
- `CAN1 / CAN2`
- `USB OTG FS`
- `GPIO`

但是 `Core\Src\main.c` 的主循环目前还是空的。这个状态非常像：

- 引脚分配已完成
- 外设框架已生成
- 业务逻辑尚未真正展开

### 4.3 硬件资料情况

原理图目录目前包含：

- `F407 main control panel.enet`
- `F407 main control panel.epro2`

同样没有导出的 PDF 或图片版原理图。

## 5. 当前系统层面的理解

结合工程名、引脚命名和启用外设，可以先得到一个比较稳的判断：

- `F407` 大概率是整个系统的主控核心。
- `F103` 大概率是灰度相关的子板或测试板。
- `K230` 和 `OpenMV` 目录很可能对应未来的视觉协处理分支，但目前还没有实质代码。
- 工作区已经预留好了日志、对话数据、备份、临时中转、资料库这些管理区，结构方向是对的。

## 6. 当前值得注意的问题

- 整个工作区缺少书面说明，后续接手或回溯成本会比较高。
- `F407` 目录名里是 `5.6`，但 `.ioc` 和 `.uvprojx` 还是 `5.1`，版本命名不一致。
- `F103` 工程没有像 `F407` 那样给大部分引脚起清晰标签，后续理解成本更高。
- 两个嵌入式工程都把大量编译产物直接放在项目内，后续备份和差异比较会比较吵。
- `K230` 和 `OpenMV` 目前还是空目录，所以涉及视觉链路的系统关系只能先做部分推断。

## 7. 建议的下一步整理动作

- 在 `dev_logs` 下继续给每个活跃模块补单独的模块说明。
- 在 `backups` 下进一步区分“源码备份”和“编译产物备份”。
- 把导出的原理图 PDF、模块照片、接线说明补进 `reference_library`。
- 尽快明确未来视觉主路线到底是 `K230` 还是 `OpenMV`，再把它和 `F407` 的通信关系固定下来。

