# F407 主控代码调研记录

- 日期：2026-05-14
- 工作区：`D:\A_5.13_SJ_HD_F407_F103_K230_XiaoChe`
- 范围：`F407 main control panel\code\AAA_F407VET6_5.6_Test`
- 目的：在正式动 `F407` 主控业务代码前，先确认当前工程结构、串口分工、已接入骨架和缺失环节

## 1. 工程结构现状

`F407` 当前代码工程位于：

`D:\A_5.13_SJ_HD_F407_F103_K230_XiaoChe\F407 main control panel\code\AAA_F407VET6_5.6_Test`

主要目录：
- `Core`
- `Drivers`
- `MDK-ARM`

当前 Keil 工程文件为：
- `MDK-ARM/F407VET6_5.1_Test.uvprojx`

## 2. 当前主程序状态

`main.c` 中已经完成：
- 外设初始化
- `gray_module` 初始化
- `vision_protocol` 初始化

但当前仍存在一个非常明确的空档：
- `while (1)` 主循环为空
- 没有任何串口接收回调
- 没有任何状态轮询、超时管理或主控业务逻辑

也就是说，当前 `F407` 还处于“协议骨架已挂进工程，但运行态链路未打通”的状态。

## 3. 当前串口/接口分工确认

结合代码命名、引脚定义和前期硬件分析，当前可确认：

### 3.1 `USART6`

- 引脚：`PC6 / PC7`
- 宏名：`CK_HD_TX / CK_HD_RX`
- 角色：连接智能灰度模块，也就是当前主用的 `F103`

这是本轮推进最自然的第一入口。

### 3.2 `UART4`

- 引脚：`PA0 / PA1`
- 宏名：`SJ_TX / SJ_RX`
- 角色：视觉信号口，对应 `OpenMV` 方向

### 3.3 `UART5`

- 引脚：`PC12 / PD2`
- 宏名：`CK_Resrve_TX / CK_Resrve_RX`
- 角色：预留串口

### 3.4 `USART3`

- 引脚：`PD8 / PD9`
- 宏名：`YJ901S_TX / YJ901S_RX`
- 角色：姿态模块链路

## 4. 现有灰度侧代码骨架状态

当前 `gray_module` 已经存在，但它的能力还停留在早期协议阶段：

- 能解析 `I2C` 寄存器块
- 能解析旧版 UART 长帧里的 `GRAY_STATUS` payload

尚未具备：
- 解析 `F103` 当前已经切换过去的 6 字节运行态短帧
- `USART6` 流式接收
- 串口中断驱动
- 在线超时维护

## 5. 现有视觉侧代码骨架状态

`vision_protocol` 已经具备比较完整的协议封装与解析骨架：

- `HELLO`
- `STATUS`
- `RESULT`
- `ACK`
- `MODE_SET / RUN_CTRL`

但目前同样只停留在模块层：
- 没有接入 `UART4` 接收路径
- 没有在主循环里建立运行态

这说明视觉链路的代码边界已经搭好，但当前不适合作为第一优先级。

## 6. 当前最主要的代码缺口

本次调研后，可以把 `F407` 当前最关键的空缺概括成四点：

1. `USART6` 没有接收中断链
2. `F103` 新短帧没有主控侧解析器
3. `GrayModuleState` 还不能保存运行态 `TURN / SCENE / FLAGS`
4. 主循环没有任何轮询和在线状态维护

## 7. 本轮改造边界建议

基于当前项目阶段，最稳妥的推进顺序不是直接进入电机、舵机或视觉融合，而是：

1. 先把 `F103 -> F407` 的 `USART6` 接收链跑通
2. 先让 `F407` 能稳定收下：
   - `TURN`
   - `SCENE`
   - `FLAGS`
3. 先把主控侧灰度状态对象维护好
4. 再在下一阶段进入：
   - 控制决策
   - 视觉协同
   - 行为层状态机

## 8. 为什么本轮先做这一步

因为当前 `F103` 已经不再发旧版长帧，而是正式切到：

```text
AA 55 TURN SCENE FLAGS CRC8
```

如果 `F407` 还继续停留在旧版 `GRAY_STATUS` 思路上，那么主从两侧已经不在同一种协议上，后续再去谈控制逻辑就没有根基。

所以本轮的第一落点必须是：
- 先让主控认得新短帧
- 先让主控真正把 `F103` 当作“运行态建议模块”接进来

## 9. 本轮动手前备份点

本轮修改前，已建立备份目录：

`D:\A_5.13_SJ_HD_F407_F103_K230_XiaoChe\backups\2026-05-14_f407_uart_short_frame_rx_start`

用于保留本轮改动前的关键文件快照，便于后续回溯和阶段对比。
